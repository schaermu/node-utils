Some utility modules i used in my projects.

Currently contains the following functionality:

* Mount AFP/SMB shared on Linux/Mac (Windows currently unsupported)
* Logging facility (for use with spark)